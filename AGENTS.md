@/Users/djabari/.codex/RTK.md

Do not create git commits unless the user explicitly asks for a commit.

When changing user-visible app behavior, add or update UI tests that exercise the running app flow, not only unit tests. Treat this as required regression coverage for bugs that only appear after launching the app.
